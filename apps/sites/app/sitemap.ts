// Dynamic sitemap — generates sitemap.xml per domain with all published translations
export default async function sitemap() {
  // Returns MetadataRoute.Sitemap with all published page URLs for the current domain
  return []
}
