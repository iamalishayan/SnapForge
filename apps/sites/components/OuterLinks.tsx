// OuterLinks — renders genuinely independent 3rd party authority links (no cross-site own domains)
export function OuterLinks() {
  return <aside>Outer links — independent authority links go here</aside>
}
