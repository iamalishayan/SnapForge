// InnerLinks — renders same-domain template links with rel="nofollow" to pass juice within one site only
export function InnerLinks() {
  return <nav>Inner links — same-domain nofollow links go here</nav>
}
