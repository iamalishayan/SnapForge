// Monitoring page — per-site GSC indexed pages, translation pipeline stats, cost summary, last 10 alerts
export default function MonitoringPage() {
  return <div>Monitoring dashboard — GSC stats and pipeline health go here</div>
}
