// Templates list — table of all templates with name, slug, category, article count, active toggle
export default function TemplatesPage() {
  return <div>Templates list — CRUD table goes here</div>
}
