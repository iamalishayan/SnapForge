// Cost dashboard — daily token usage chart, estimated cost by model, monthly projection from cost_log
export default function CostsPage() {
  return <div>Cost dashboard — token usage charts and projections go here</div>
}
