// Dashboard — stats cards (templates, articles by status, sites live, pages indexed), recent publish log, cost this month
export default function DashboardPage() {
  return <div>Dashboard — stats and overview go here</div>
}
