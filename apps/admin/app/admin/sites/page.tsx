// Sites config — table of all site_configs with domain, language, theme, active toggle, add/edit form
export default function SitesPage() {
  return <div>Sites config — CRUD table for site_configs goes here</div>
}
