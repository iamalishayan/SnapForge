#!/usr/bin/env tsx
// add-site.ts — onboarding script: inserts site_config, seeds keywords, triggers translation pipeline for all templates
// Usage: npx tsx scripts/add-site.ts --domain ki-foto.de --language de --country DE --theme theme_b --monetization affiliate

console.log('add-site script — implement site onboarding logic here')
