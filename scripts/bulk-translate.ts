#!/usr/bin/env tsx
// bulk-translate.ts — triggers translation pipeline for all articles × all sites in a single batch run (dev/migration use)
console.log('bulk-translate script — implement batch translation trigger here')
